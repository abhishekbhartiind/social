query Replies($parentCommentId: ID!) {
    replies(parentCommentId: $parentCommentId) {
        content
        postId
        authorId
        author {
            username
            email 
            id
        }
        id
    }
}