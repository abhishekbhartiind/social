query Posts($limit: Int!, $cursor: String) {
    posts(limit: $limit, cursor: $cursor) {
        hasMore
        posts {
            createdAt
            creator {
                email
                id
                username
            }
            creatorId
            id
            isLiked
            likeCount
            text
            title
            updatedAt
        }
    }
}